import 'package:flutter/material.dart';

class KakaoRoom extends StatelessWidget {
  const KakaoRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Hello World')),
    );
  }
}
